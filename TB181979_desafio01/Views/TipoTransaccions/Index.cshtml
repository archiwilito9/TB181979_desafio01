@model IEnumerable<TB181979_desafio01.Models.TipoTransaccion>

@{
    ViewBag.Title = "Index";
}

<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-filter-square-fill" viewBox="0 0 16 16">
    <path d="M2 0a2 2 0 0 0-2 2v12a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V2a2 2 0 0 0-2-2H2zm.5 5h11a.5.5 0 0 1 0 1h-11a.5.5 0 0 1 0-1zM4 8.5a.5.5 0 0 1 .5-.5h7a.5.5 0 0 1 0 1h-7a.5.5 0 0 1-.5-.5zm2 3a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 0 1h-3a.5.5 0 0 1-.5-.5z" />
</svg>
<h2 class="d-inline-block">Tipo de transacciones</h2>

<p>
    @Html.ActionLink("Agregar tipo de transacción", "Create", "", new { @class = "btn btn-info" })
</p>
<table class="table table-hover table-dark">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tipo_Transaccion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tipo_Transaccion)
        </td>
        <td>
            @Html.ActionLink(" ", "Edit", new { id=item.id }, new { @class = "btn btn-warning bi bi-pencil" }) |
            @Html.ActionLink(" ", "Details", new { id=item.id }, new { @class = "btn btn-info bi bi-file-earmark-spreadsheet" }) |
            @Html.ActionLink(" ", "Delete", new { id=item.id }, new { @class = "btn btn-danger bi bi-trash" })
        </td>
    </tr>
}

</table>
